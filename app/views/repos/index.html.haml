.row
  .col-md-6.col-md-offset-3
    %h3.text-center.white
      Your Repositories
    %ul.list-unstyled.pagination.repo_list
      - @repos.each do |repo|
        %li
          .panel.panel-body.added-repo.slategrey
            .row
              .col-md-10
                = link_to repo.name, repo_path(repo), class: 'btn btn-default btn-large repo-name-button'
              .col-md-2
                .pull-right
                  = link_to '-', repo_path(repo.name), method: :delete, class: "btn btn-default btn-small delete-#{repo.name}-button"

