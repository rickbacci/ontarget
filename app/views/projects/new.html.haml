.row
  .col-md-6.col-md-offset-3
    %h3.text-center.white
      Your Repositories
    %ul.list-unstyled.pagination.project_list
      - @repos.each do |repo|
        - next if repo.nil?
        %li
          - if !repo.nil? && added_to_projects(repo.name)
            .panel.panel-body.added-project#new-projects
              .pull-left
                = repo.name
              .pull-right
                = link_to '-', project_path(repo.name), method: :delete, class: 'btn btn-default btn-small minus-button'
          -else
            .panel.panel-body#new-projects
              .pull-left
                = repo.name
              .pull-right
                = link_to '+', {controller: "projects", action: "create", name: repo.name }, method: :post, class: "#{repo.name} btn btn-default btn-small"

